import { SETTINGS } from 'core/settings';

const prefix = SETTINGS.prefixStorageKey;

export const TIMER_KEY = `${prefix}TIMER_KEY`;
export const INIT_VALUES_FORM_KEY = `${prefix}INIT_VALUES_FORM_KEY`;
export const VERSION_VIEW_VISIBLE_KEY = `${prefix}VERSION_VIEW_VISIBLE`;
